apiVersion: v1
kind: Service
metadata:
  name: {{ include "sms-relay.fullname" . }}
  labels:
    {{- include "sms-relay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "sms-relay.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
